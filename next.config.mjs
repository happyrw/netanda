/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["m.media-amazon.com", "image.tmdb.org", "www.fillmurray.com", "imgage.tmdb.org"],
    }
};

export default nextConfig;
